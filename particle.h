#include<stdio.h>
//base code of the programming but that doesn't make sense first these two distinctly differnet particles must be written 
//
int particleParticle(int);
int changeParticle(int);
int particleParticles(int);
void radioactiveDecay(int);
double constant(int , double, double);
