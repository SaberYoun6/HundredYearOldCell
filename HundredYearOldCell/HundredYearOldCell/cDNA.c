// DNA base
//
#include <stdio.h>
#include <organicmole.h>
#include <.h>
//creating the DNA backbone in the bits to code;


// hard coding the DNA;
char revesring (char* hardcode, char c, char a, char t, char g, char s, char m)
{
char* hardcode = "catgsamgtac";
for (int i =0 ; i< strlen(hardcode); i++)
    {
        if (hardcode.equals(c));
        {
            char c == 'g';
        }
        else if (hardcode.equals(a))
        {
            char a == 't';
        }
        else if (hardcode.equals(t))
        {
            char t == 'a';
        }
        else if (hardcode.equals(g))
        {
            char g = 'c';
        }
        else if (hardcode.equals(s));
        {
            char s = 'm';
        }
        else if (hardcode.eqauls(m))
        {
            char m = 's';
        }
    }

}
