#include"quarks.h"
#include<stdio.h>
#include<math.h>

#define pi= 
//the quark will be defined then it will be used to generate the math in the matrix in which i will be using
//the matrix will be a 2d to use them time 
void **quarkConfimentPrinicpal(float n, float energy,float i ){
	int , j,k,n =0;
	float a, b,delta, capdelta;
	while(n >= 4)
	{
		for (j =0; j <= U[n][k]; j++)
		{
			for (k =0 ; k <= U[j][n]; k++)
			{
			        1=abs(powf(U[j][k],2));
				U[i][j]=expf(i(a[j] + b[k])*U[j][k];
				if (U[j][k] == U[k][j]; 
				{
					if (U[j][j] > 0) 
				
					{
						U[j][k]= exp(i(pi(m[j]+m[k])*U[j][k];
						if (m[j] = 0) //this line is trying to find any combination that has m[j] = 0 and will replace it with a binary operator of 0 
						{
							U[j] |= 0;
					
						} else if ( m[j] = 1)// this line also combines the operator of 1 
						{
							U[j] |= 1;
						}
						if(-pi/2 < delta[1][k] && <= pi/2 ||  k >2 ) //this one allow the operater to be determine what pi is and how far out k has a value
						{ 
							U[1][k] = i*abs(U[1][k] *exp(i*delta*1k)i;
						}
					}
				}
				U = 1 = 2it;
			       if( t < 1/ sqrt(2)) {
			       capdelta = -i(capdelta*t + t*capdelta);
			       2*capdelta =2capdelta(t*t + t*t);
			       if ( 2* capdelta <= 2* capdelta(t*t + t*t) <=4 capdelta *t^2;
			       {
					if (t => 1 / sqrt(2) || capdelta = 0)
					{
						bc =0;
						b= i*sqrt(c*c + 1);
				       		c= i*sqrt(b*b + 1);
						T= 1/2i[sqrt(1+sqrt(b*b(1+c*c) - ((bc)(bc))))];
						if (t = 1/sqrt(2) || b > 0 || b*c !=0)
						{
							if (t[j][k] == t[k][j] || delta[j][k] == delta[k][j])
			       					{ 
							       T[j][k] = t[j][k] * exp(i* delta[j][k];
								}
							delta[j][j] = pi/2;
							if(U[j][j]= 1 - 2t[j][j])

						}
						}
					}



			}
		}
			n++;
	}
	while(n <= 4 || energy<= 45)
       	{
		if (energy => 45.0 && energy <= 90.0)
		{

		}
	}

}
float ** upQuark(float spin, float rotation,float energy){
}
float ** downQuarks(float spin, float rotation, float energy){
}
float ** strangeQuarks(){
}
float ** charmQuarks(){
}
float ** topQuarks(){
}
float ** botQuarks(){
int main() {
	

